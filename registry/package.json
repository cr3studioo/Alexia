{
  "name": "@alexia/registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "description": "A list with a revoke button. Cloudflare Workers + D1.",
  "scripts": {
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute alexia --file=src/schema.sql --remote"
  }
}
