{
  "$schema": "../../docs/spec/plugin.schema.json",
  "manifest_version": 1,
  "id": "telegram",
  "name": "Telegram",
  "summary": "Talk to Alexia from Telegram. Every message crosses Telegram's servers, and every reply says so.",
  "version": "0.4.0",
  "license": "AGPL-3.0-only",
  "entry": {
    "run": "node",
    "args": [
      "index.js"
    ]
  },
  "alexia_protocol": 7,
  "mcp_protocol": "2025-11-25",
  "requires": [
    {
      "cap": "net.request",
      "why": "To hold a connection open to Telegram and collect the messages you send it."
    },
    {
      "cap": "fs.own_dir",
      "why": "To hold a voice note or a file you sent for a moment while it is turned into words, then drop it."
    }
  ],
  "provides": [
    "telegram.send",
    "ask.confirm"
  ],
  "settings": [
    {
      "type": "password",
      "key": "bot_token",
      "label": "Bot token",
      "hint": "From @BotFather on Telegram. Send it /newbot and it hands you one. It goes to this machine's credential store, never to the database."
    },
    {
      "type": "choice",
      "key": "voice_replies",
      "label": "Answer with a voice note",
      "default": "mirror",
      "hint": "Any of these needs a cloned voice chosen in Voice — that is the one that makes the format a voice bubble takes. Without it, replies are text whatever is set here.",
      "options": [
        { "value": "never", "label": "Never", "hint": "Every reply is text, even to a voice note." },
        { "value": "mirror", "label": "When you send a voice note", "hint": "The reply comes back in the form the question went out in: speak and she speaks, type and she types." },
        { "value": "always", "label": "Always", "hint": "Every reply is a voice bubble, including the answer to something you typed." }
      ]
    },
    {
      "type": "text",
      "key": "morning_summary",
      "label": "Send what is due each morning at",
      "placeholder": "08:30",
      "hint": "A time in 24-hour form, in this machine's own clock. Leave it empty for no summary. It needs a plugin that keeps track of what you owe — without one there is nothing to list, so no summary goes out."
    },
    {
      "type": "text",
      "key": "panel_url",
      "label": "Control panel page",
      "default": "https://cr3studioo.github.io/Alexia/telegram/",
      "hint": "The page behind the ⚙ Panel button. It is one static file with no server behind it: the state it shows travels in the part of the link a browser never sends to the host, and the page itself calls nothing. The default address only exists once GitHub Pages has been switched on for the repository, which is a one-time step somebody has to do by hand."
    },
    {
      "type": "text",
      "key": "ntfy_topic",
      "label": "ntfy topic, if Telegram is down",
      "placeholder": "something-nobody-would-guess",
      "hint": "A fallback and never a replacement: no buttons, no threading, and anyone who knows the topic can read it. Leave it empty and nothing is sent anywhere else."
    },
    {
      "type": "status",
      "key": "state",
      "label": "State"
    },
    {
      "type": "status",
      "key": "pairing_code",
      "label": "Pairing code",
      "hint": "Send this code to your bot from the Telegram account you want to allow. Nobody else is answered."
    },
    {
      "type": "action",
      "key": "unpair",
      "label": "Forget who is allowed",
      "tool": "unpair",
      "hint": "Clears the allowed accounts and makes a new pairing code. Do this if the old one got out."
    }
  ],
  "storage": {
    "namespace": "telegram",
    "tables": [
      "chats",
      "reminders"
    ]
  },
  "commands": [
    {
      "name": "paired",
      "summary": "Who is allowed to reach Alexia from Telegram"
    }
  ],
  "lifetime": "resident"
}
