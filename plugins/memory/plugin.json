{
  "$schema": "../../docs/spec/plugin.schema.json",
  "manifest_version": 1,
  "id": "memory",
  "name": "Long-term memory",
  "summary": "Remembers things across conversations — what you told it last month, and where to find it again. Can also write things down by itself, once you turn that on.",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "entry": {
    "run": "node",
    "args": [
      "index.js"
    ]
  },
  "alexia_protocol": 3,
  "mcp_protocol": "2025-11-25",
  "requires": [],
  "provides": [
    "memory.remember",
    "memory.recall",
    "memory.capture"
  ],
  "settings": [
    {
      "type": "number",
      "key": "recall_limit",
      "label": "How much to bring back",
      "min": 1,
      "max": 20,
      "step": 1,
      "default": 6,
      "hint": "How many remembered things a search returns at most. More is more context and more cost."
    },
    {
      "type": "toggle",
      "key": "capture",
      "label": "Write things down by itself",
      "default": false,
      "hint": "Off, Alexia only remembers what it is told to. On, every finished exchange is kept briefly and turned into notes on a timer — nothing leaves this machine that would not have anyway, and credentials are stripped before it ever sees them."
    },
    {
      "type": "number",
      "key": "interval",
      "label": "How often to sort through it",
      "min": 1,
      "max": 240,
      "step": 1,
      "default": 12,
      "hint": "Minutes between passes. An empty buffer costs nothing — no model is called when there is nothing to sort."
    },
    {
      "type": "status",
      "key": "state",
      "label": "State"
    },
    {
      "type": "action",
      "key": "sort_now",
      "label": "Sort through it now",
      "tool": "sort_now",
      "hint": "Do the next pass now instead of waiting for the timer. Does nothing if there is nothing waiting."
    },
    {
      "type": "action",
      "key": "forget_all",
      "label": "Forget everything",
      "tool": "forget_all",
      "hint": "Empties long-term memory. The conversation you are having now is not touched — that belongs to Alexia, not to this plugin."
    }
  ],
  "storage": {
    "namespace": "memory",
    "tables": [
      "facts",
      "buffer",
      "forgotten"
    ]
  },
  "commands": [
    {
      "name": "remembered",
      "summary": "What Alexia remembers about you"
    }
  ],
  "panel": {
    "label": "Memory",
    "widgets": [
      {
        "key": "remembered_list",
        "type": "table",
        "label": "Remembered",
        "hint": "Everything Alexia has written down about you, and whether you said it or it worked it out. Forgetting one is the reason this screen exists.",
        "rows": "memories",
        "columns": [
          {
            "key": "text",
            "label": "What"
          },
          {
            "key": "from",
            "label": "How it got here"
          },
          {
            "key": "when",
            "label": "Written down",
            "align": "right",
            "hideNarrow": true
          }
        ],
        "rowActions": [
          {
            "key": "forget_one",
            "label": "Forget",
            "tool": "forget_one",
            "confirm": "Forget it?"
          }
        ],
        "detail": "about_memory",
        "filter": true,
        "groupBy": "kind"
      }
    ]
  },
  "lifetime": "resident"
}
